# Document ingestion
pdfplumber==0.11.0
PyMuPDF>=1.24.0
pytesseract==0.3.13        # OCR for scanned PDFs
pdf2image==1.17.0          # Convert PDF pages to images for OCR
boto3==1.34.0              # AWS Textract for OCR fallback (production)

# Embedding (production only — set USE_VECTOR_STORE=true in .env)
# voyageai==0.2.3

# Vector store (production only)
# supabase==2.5.0
# vecs==0.4.3

# LLM
anthropic==0.28.0

# Output
reportlab==4.2.2           # PDF report generation
pydantic>=2.9.0

# API
fastapi==0.111.0
uvicorn==0.30.0
python-multipart==0.0.9    # File upload support
jinja2==3.1.4              # HTML templates
aiofiles==23.2.1           # Async static file serving

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utils
python-dotenv==1.0.1
pyyaml==6.0.1
loguru==0.7.2
httpx==0.27.0              # Async HTTP client
